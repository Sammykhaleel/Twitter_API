<%= form_for(@twitterfeed) do |f| %>
  <% if @twitterfeed.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@twitterfeed.errors.count, "error") %> prohibited this twitterfeed from being saved:</h2>

      <ul>
      <% @twitterfeed.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
